# Methodology

The intended workflow is: inspect the supplied schema, normalize comparable
fields, generate blocked candidate pairs, compute pairwise similarities, train
or calibrate a matcher, evaluate it, and write the competition submission.

